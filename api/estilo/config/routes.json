{
  "routes": [
    {
      "method": "GET",
      "path": "/estilos",
      "handler": "estilo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/estilos/count",
      "handler": "estilo.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/estilos/:id",
      "handler": "estilo.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/estilos",
      "handler": "estilo.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/estilos/:id",
      "handler": "estilo.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/estilos/:id",
      "handler": "estilo.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
